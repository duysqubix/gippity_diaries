services:
  gippity-diaries:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ${JOURNAL_FILE:-./example/JOURNAL.md}:/journal/JOURNAL.md:ro
    environment:
      - JOURNAL_PATH=/journal/JOURNAL.md
    restart: unless-stopped
